# Demo

This folder contains small automation-only demos and smoke tests.

Current runner:
- `run-smoke-test.cjs`
- `run-smoke-test.ts`

Coverage:
- `catalog.read` through the execution service
- `asset.search` through the execution service
- one no-ready-provider case through the execution service

Suggested run command:

```bash
node automation/demo/run-smoke-test.cjs
```
